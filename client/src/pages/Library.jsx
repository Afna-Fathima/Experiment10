import React from 'react'

export default function Library(){
  return (
    <div>
      <h2 className="text-xl font-semibold mb-4">Your Library</h2>
      <p className="text-gray-600">(Subscribe and liked items will appear here — requires Firebase auth integration.)</p>
    </div>
  )
}
